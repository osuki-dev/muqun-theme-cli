---
'@osuki-dev/muqun-theme': patch
---

Support optional `iconDirections` metadata so one theme arrow can be rotated correctly for Home quick actions and the Gateway picker. Authors declare the direction actually drawn into the image; older Apps ignore this metadata and keep their existing icon behavior.
